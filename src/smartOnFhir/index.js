/**
 * SMART on FHIR Module Exports
 */
const { SmartConfigurationEndpoint } = require('./smartConfigurationEndpoint');
const { BackendServicesAuth } = require('./backendServicesAuth');

module.exports = {
    SmartConfigurationEndpoint,
    BackendServicesAuth
};
